node_modules
.next
.syncore-data
# Session workspace, not project state. Untracked-but-not-ignored made local
# lint/typecheck fail on files CI never sees — two signals disagreeing for no reason.
scratchpad
out
dist
coverage
test-results
playwright-report
*.tsbuildinfo
.env
.env.local
.env.*.local
# Real provisioning config holds team PII — keep it out of git (example is committed).
scripts/provisioning/accounts.json
scripts/provisioning/*.local.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store

# Lead spreadsheets dropped in for import. Real contact PII — never commit.
*.xlsx
*.xls

# Lead CSVs dropped in for import. Real business contact data — never commit.
docs/*.csv
